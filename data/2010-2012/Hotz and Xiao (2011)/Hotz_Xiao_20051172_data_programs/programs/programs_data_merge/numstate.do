* this program creates a State codes
	gen numstate=.
	replace numstate=1  if st=="AL"
	replace numstate=2  if st=="AK"
	*replace numstate=3  if st==""
	replace numstate=4  if st=="AZ"
	replace numstate=5  if st=="AR"
	replace numstate=6  if st=="CA"
	*replace numstate=7  if st==""
	replace numstate=8  if st=="CO"
	replace numstate=9  if st=="CT"
	replace numstate=10  if st=="DE" 
	replace numstate=11  if st=="DC"
	replace numstate=12  if st=="FL"
	replace numstate=13  if st=="GA"
	*replace numstate=14  if st==""
	replace numstate=15  if st=="HI"
	replace numstate=16  if st=="ID"
	replace numstate=17  if st=="IL"
	replace numstate=18  if st=="IN"
	replace numstate=19  if st=="IA"
	replace numstate=20  if st=="KS"
	replace numstate=21  if st=="KY"
	replace numstate=22  if st=="LA"
	replace numstate=23  if st=="ME"
	replace numstate=24  if st=="MD"
	replace numstate=25  if st=="MA"
	replace numstate=26  if st=="MI"
	replace numstate=27  if st=="MN"
	replace numstate=28  if st=="MS"
	replace numstate=29  if st=="MO"
	replace numstate=30  if st=="MT"
	replace numstate=31  if st=="NE"
	replace numstate=32  if st=="NV"
	replace numstate=33  if st=="NH"
	replace numstate=34  if st=="NJ"
	replace numstate=35  if st=="NM"
	replace numstate=36  if st=="NY"
	replace numstate=37  if st=="NC"
	replace numstate=38  if st=="ND"
	replace numstate=39  if st=="OH"
	replace numstate=40  if st=="OK"
	replace numstate=41  if st=="OR"
	replace numstate=42  if st=="PA"
	*replace numstate=43  if st==""
	replace numstate=44  if st=="RI"
	replace numstate=45  if st=="SC"
	replace numstate=46  if st=="SD"
	replace numstate=47  if st=="TN"
	replace numstate=48  if st=="TX"
	replace numstate=49  if st=="UT"
	replace numstate=50  if st=="VT"
	replace numstate=51  if st=="VA"
	*replace numstate=52  if st==""
	replace numstate=53  if st=="WA"
	replace numstate=54  if st=="WV"
	replace numstate=55  if st=="WI"
	replace numstate=56  if st=="WY"
	*replace numstate=57  if st=="WI"
	*replace numstate=58  if st=="WY"
